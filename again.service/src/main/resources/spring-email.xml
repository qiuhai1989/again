<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
			http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans-3.2.xsd">


	<!-- 邮件的发送器，非常的重要 ，传入配置文件的信息-->
	<bean name="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
		<property name="host">
			<value>${mail.smtp.host}</value>
		</property>
		<property name="username">
			<value>${mail.smtp.username}</value>
		</property>
		<property name="password">
			<value>${mail.smtp.password}</value>
		</property>
	
		<property name="port">
			<value>${mail.smtp.port}</value>
		</property>
        
		<property name="javaMailProperties">
			<props>
				<prop key="mail.smtp.auth">${mail.smtp.auth}</prop>
				<prop key="mail.smtp.timeout">${mail.smtp.timeout}</prop>
				<prop key="mail.smtp.starttls.enable">${mail.smtp.starttls.enable}</prop>
				<!-- 
				<prop key="mail.smtp.socketFactory.class">${mail.smtp.socketFactory.class}</prop>
				<prop key="mail.smtp.socketFactory.port">${mail.smtp.socketFactory.port}</prop>
				<prop key="mail.smtp.socketFactory.fallback">${mail.smtp.socketFactory.fallback}</prop> -->
			</props>

		</property>
	</bean>

	<!-- 邮件发送工具，也是一个基础的模板类，其他类型的模板都是"继承"这个的 -->
	<bean name="baseMailTemplate" class="com.haqiu.core.utils.VelocityMailUtil"
		abstract="true">
		<property name="velocityEngine" ref="velocityEngine" />
		<property name="mailHeaders">
			<props>
				<prop key="Content-Type">
					${mail.default.contentType}
				</prop>
			</props>
		</property>
		<property name="from">
			<value>${mail.default.from}</value>
		</property>
	</bean>

	<!-- 读取velocity模板的工厂类，也就是的不同的模板的 -->
	<bean id="velocityEngine"
		class="org.springframework.ui.velocity.VelocityEngineFactoryBean">
		<property name="velocityProperties">
			<props>
				<!-- 设置从classPath下读取模板文件 vm文件 -->
				<prop key="resource.loader">class</prop>
				<prop key="class.resource.loader.class">
					org.apache.velocity.runtime.resource.loader.ClasspathResourceLoader
				</prop>
				<prop key="velocimacro.library"></prop>
			</props>
		</property>
	</bean>
	
	<bean id="welcomeMailTemplate" parent="baseMailTemplate">
		<property name="subject">
			<value>${mail.welcome.subject}</value>
		</property>
		<property name="templateName">
			<value>${mail.welcome.template}</value>
		</property>
	</bean>
	
	<bean id="resetMailTemplate" parent="baseMailTemplate">
		<property name="subject">
			<value>${mail.reset.subject}</value>
		</property>
		<property name="templateName">
			<value>${mail.reset.template}</value>
		</property>
	</bean>
	

</beans>